<?xml version="1.0" encoding="utf-8"?>
<!-- ============================================================
     COMPONENTE REUTILIZÁVEL — item_categoria.xml
     Usado com <include> no activity_dashboard.xml
     Preencher via código: tvNomeCategoria, tvValor, tvPercent,
     ivIconeCategoria, progressBar
     ============================================================ -->
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:layout_marginBottom="18dp">

    <!-- Linha: ícone + nome + valor + percentual -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:layout_marginBottom="8dp">

        <!-- Ícone da categoria -->
        <LinearLayout
            android:layout_width="34dp"
            android:layout_height="34dp"
            android:background="@drawable/bg_icon_categoria"
            android:gravity="center"
            android:layout_marginEnd="12dp">

            <ImageView
                android:id="@+id/ivIconeCategoria"
                android:layout_width="18dp"
                android:layout_height="18dp"
                android:src="@drawable/ic_home"
                android:tint="#10B981"
                android:contentDescription="@null" />
        </LinearLayout>

        <!-- Nome da categoria -->
        <TextView
            android:id="@+id/tvNomeCategoria"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="Categoria"
            android:textSize="14sp"
            android:textColor="#111827" />

        <!-- Valor -->
        <TextView
            android:id="@+id/tvValorCategoria"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="R$ 0,00"
            android:textSize="14sp"
            android:textStyle="bold"
            android:textColor="#111827"
            android:layout_marginEnd="12dp" />

        <!-- Percentual -->
        <TextView
            android:id="@+id/tvPercentCategoria"
            android:layout_width="40dp"
            android:layout_height="wrap_content"
            android:text="0%"
            android:textSize="13sp"
            android:textColor="#6B7280"
            android:gravity="end" />
    </LinearLayout>

    <!-- Barra de progresso -->
    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="6dp">

        <!-- Fundo -->
        <View
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:background="@drawable/bg_progress_track" />

        <!-- Progresso preenchido -->
        <View
            android:id="@+id/progressFill"
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:background="@drawable/bg_progress_fill" />
    </FrameLayout>

</LinearLayout>